# Repository tests

**Owner:** Quality engineering

**Purpose:** Hold cross-package checks that enforce repository-wide boundaries. Package-specific behavior tests should live with their owning package when those suites are introduced.
