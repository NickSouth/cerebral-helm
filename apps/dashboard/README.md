# Dashboard

**Owner:** Dashboard

**Purpose:** Production React and TypeScript presentation hosted by the Pre-Mac development server and, later, WKWebView.

The dashboard may render state and express intent through `CerebralBridge`. It must not import filesystem, process, database, native-platform, model, or provider APIs. Initial tooling and the runnable shell are deferred to NIC-12.
