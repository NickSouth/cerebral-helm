{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cerebralhelm.local/schemas/tools/system-status-read-input.schema.json",
  "title": "CerebralHelm System Status Read Input",
  "type": "object",
  "additionalProperties": false,
  "required": [],
  "properties": {
    "metrics": {
      "description": "Which metrics to read. Omitted or empty reads every supported metric; name a subset when the user asked about specific ones. A requested metric the host cannot supply comes back with an explicit unavailable state rather than being dropped, so a missing entry never has to be inferred.",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": ["cpu", "memory", "network", "battery", "display"]
      }
    }
  }
}
