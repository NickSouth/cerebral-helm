{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cerebralhelm.local/schemas/tools/note-search-input.schema.json",
  "title": "CerebralHelm Note Search Input",
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": {
      "description": "Free text matched against note titles, metadata, and Markdown content. Use the user's own search terms; this is a literal text match, not a semantic one, so paraphrasing the user's wording reduces the chance of a hit.",
      "type": "string",
      "minLength": 1
    },
    "limit": {
      "description": "Caps the returned hits. Omitted means every match in the vault, which for a common word can be most of the library. Hits are ordered by note path, not by relevance, so a cap truncates alphabetically rather than keeping the best matches - the output reports `truncated`, so a caller is never silently shown a partial result.",
      "type": "integer",
      "minimum": 1
    }
  }
}
