{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cerebralhelm.local/schemas/tools/url-open-input.schema.json",
  "title": "CerebralHelm Url Open Input",
  "type": "object",
  "additionalProperties": false,
  "required": ["urlId"],
  "properties": {
    "urlId": {
      "description": "The id of a CONFIGURED URL reference, from the URL reference catalog — never a literal web address. To open an arbitrary https address the user supplied, use `web.open` instead; this tool only opens destinations that were configured ahead of time. If the user's words do not resolve to a known id, ask rather than guessing.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "minLength": 1
    }
  }
}
