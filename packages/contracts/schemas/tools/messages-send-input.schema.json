{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cerebralhelm.local/schemas/tools/messages-send-input.schema.json",
  "title": "CerebralHelm Messages Send Input",
  "type": "object",
  "additionalProperties": false,
  "required": ["messageBody", "messageTarget", "messageTargetKind"],
  "properties": {
    "messageBody": {
      "type": "string",
      "minLength": 1,
      "description": "The message text. Passed to the adapter as an argument, never interpolated into a script, so quotes and AppleScript keywords in it are data."
    },
    "messageTarget": {
      "type": "string",
      "minLength": 1,
      "description": "A participant handle (phone number or Apple ID) or a chat identifier, per messageTargetKind."
    },
    "messageTargetKind": {
      "type": "string",
      "enum": ["participant", "chat"],
      "description": "Messages can send to one person or to an existing chat. A new group cannot be assembled — the scripting dictionary's `chat` class is read-only — so a group is always an existing thread."
    },
    "messageTargetName": {
      "type": "string",
      "description": "The recipient's display name, so a confirmation can name who this is going to in words rather than as a phone number."
    },
    "messageGroupSize": {
      "type": "integer",
      "minimum": 2,
      "description": "How many people are in the thread, when it is a group. Disclosed because sending to nine people is a materially bigger action than sending to one (FR-SAF-04)."
    }
  }
}
