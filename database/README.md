# Operational database

**Owner:** Storage

**Purpose:** Define ordered SQLite migrations and storage documentation for operational state. Full knowledge bodies remain authoritative in Markdown.

The `db-reset` command currently targets `.local/development/database/cerebral.sqlite` only. It prepares and clears the dedicated development path without applying schema until storage implementation arrives.
