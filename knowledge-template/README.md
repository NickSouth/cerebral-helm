# Knowledge template

**Owner:** Knowledge

**Purpose:** Seed the user-owned Markdown hierarchy without making repository content authoritative over user data.

Updates may add optional templates but must never overwrite a user's knowledge tree.
