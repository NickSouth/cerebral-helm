# archive

Completed or inactive notes moved out of the active folders. Archiving sets the
note `status` to `archived` and relocates it here, keeping the active folders
focused while preserving history. Nothing is deleted on archive.
