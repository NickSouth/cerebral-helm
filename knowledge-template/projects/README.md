# projects

Goal-bound efforts with a defined end state. Each project is a durable folder (or
note) referenced by `project` id. Modes point at projects through `projectHints`;
the note lives here once and is referenced, never duplicated per mode. Completed
projects move to `archive/`.
