# areas

Ongoing responsibilities with no end state (e.g. health, finances, a role). Like
projects, areas are durable and referenced by `project` id from modes — they are
not duplicated per mode. An area that becomes inactive moves to `archive/`.
