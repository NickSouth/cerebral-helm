{
  "schemaVersion": "1.0.0",
  "id": "note.list",
  "version": "1.0.0",
  "purpose": "List the durable Markdown notes under the knowledge root, reading the files themselves so a note authored outside CerebralHelm is listed before any index rebuild.",
  "inputSchema": {
    "schemaId": "https://cerebralhelm.local/schemas/tools/note-list-input.schema.json",
    "schemaVersion": "1.0.0"
  },
  "outputSchema": {
    "schemaId": "https://cerebralhelm.local/schemas/tools/note-list-output.schema.json",
    "schemaVersion": "1.0.0"
  },
  "risk": "read_only",
  "runtimeRiskPolicy": "descriptor_risk",
  "confirmationPolicyKey": "allow_read_without_confirmation",
  "requiredPermissions": ["knowledge_root_read"],
  "secretReferences": [],
  "timeoutMs": 5000,
  "retry": {
    "maxAttempts": 0,
    "strategy": "none"
  },
  "cancellable": true,
  "idempotency": "idempotent",
  "adapterRequirements": {
    "capabilities": ["note.list"],
    "platforms": ["pre_mac_mock", "macos_native"]
  },
  "logging": {
    "redactionPaths": ["/notes"],
    "retention": "standard"
  },
  "results": ["success", "partial_success", "unavailable", "timeout", "failure", "cancelled"],
  "availability": {
    "preMac": true,
    "macOS": true
  }
}
